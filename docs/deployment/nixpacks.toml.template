# Nixpacks 설정 파일 (Railway용)
# 프로젝트 루트에 nixpacks.toml로 복사하여 사용

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run verify"]

[start]
cmd = "npm start"

# 환경 변수
[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
